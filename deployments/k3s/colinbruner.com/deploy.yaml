apiVersion: apps/v1
kind: Deployment
metadata:
  name: web
  namespace: homepage
  labels:
    app: web
spec:
  replicas: 5
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 20%
  selector:
    matchLabels:
      app: web
  template:
    metadata:
      labels:
        app: web
    spec:
      imagePullSecrets:
        - name: registrykey
      containers:
        - name: web
          image: cbruner/homepage:v1.0.2
          ports:
            - containerPort: 80
            - containerPort: 443
          volumeMounts:
            - name: tls-volume
              mountPath: /etc/nginx/secrets
          readinessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 5
            periodSeconds: 5
            successThreshold: 1
      volumes:
        - name: tls-volume
          secret:
            secretName: homepage-tls
